#Estas son todas las lista y matrices que seran utilizadas
Pizzerias = ['Pizza Hut', 'Dominos Pizza', 'Papa Johns']
Distritos = ['Surco', 'Miraflores', 'San Isidro', 'La Molina']
Surco = ['Pizza Hut', 'Dominos Pizza', 'Papa Johns']
Miraflores = ['Pizza Hut', 'Dominos Pizza', 'Papa Johns']
San_Isidro = ['Pizza Hut', 'Dominos Pizza', 'Papa Johns']
La_Molina = ['Pizza Hut', 'Dominos Pizza', 'Papa Johns']
Numeros_telefonicos = [ ['(01) 5051111', '(01) 5051111', '(01) 5051111', '(01) 74-9497'],
                        ['(01) 3211100', '(01) 4463777', '(01) 3914700]', '(01) 7155676'],
                        ['(01) 606-0000', '(01) 606-0000', '(01) 606-0000', '(01) 606-0000'] ]
Pizzas = ['Americana', 'Hawaiana', 'Pepperoni']
Bebidas = ['Coca-Cola', 'Sprite', 'Inca Kola']
Precios_pizza = [ [41.90, 46.90, 41.90],
                  [39.90, 46.90, 46.90],
                  [41.90, 48.90, 40.00] ]

precios_bebida = [ [5.00, 3.50, 5.00],
                   [9.90, 3.90, 9.90],
                   [9.70, 3.70, 9.70] ]

dic_orden = {}
#Aqui pide el distrito en donde vives (Esta valor no tiene mucho uso en la version actual, pero sera importante para la entrega 3)
distrito = input("Ingrese el Distrito en donde vive (Surco, Miraflores, San Isidro, La Molina): ")
while True:
    if distrito in Distritos:
        break
    else:
        print('Lo sentimos, no contamos con información de ese distrito')
    distrito = input("Ingrese un distrito cercano (Surco, Miraflores, San Isidro, La Molina): ")

#Aqui se elige una pizzeria (Tomando en cuenta el distrito)
pizzeria = input('Escoge una pizzeria (Pizza Hut, Dominos Pizza, Papa Johns): ')
while True:
    if distrito == 'Surco':
        if pizzeria in Surco:
            break
        else:
            print('Lo sentimos, no existe esa pizzaria en', distrito)
            pizzeria = input('Escoge otro pizzeria(Pizza Hut, Dominos Pizza, Papa Johns): ')
    if distrito == 'Miraflores':
        if pizzeria in Miraflores:
            break
        else:
            print('Lo sentimos, no existe esa pizzaria en', distrito)
            pizzeria = input('Escoge otro pizzeria(Pizza Hut, Dominos Pizza, Papa Johns): ')
    if distrito == 'San Isidro':
        if pizzeria in San_Isidro:
            break
        else:
            print('Lo sentimos, no existe esa pizzaria en', distrito)
            pizzeria = input('Escoge otro pizzeria(Pizza Hut, Dominos Pizza, Papa Johns): ')
    if distrito == 'La Molina':
        if pizzeria in La_Molina:
            break
        else:
            print('Lo sentimos, no existe esa pizzaria en', distrito)
            pizzeria = input('Escoge otro pizzeria(Pizza Hut, Dominos Pizza, Papa Johns): ')

print('Ahora empezaremos  a armar su orden')
seguir_pidiendo = 'si'
#Con este bucle te pide que pizzas quieres y la cantidad de esta, hasta que el usuario ya no quiera ordenar más (Se guarda en un diccionario)
while seguir_pidiendo.lower() == 'si':
    pizza = input('Ingrese que pizza quiere (Americana, Hawaiana, Pepperoni): ')
    if pizza not in Pizzas:
        print('Lo sentimos, no contamos con esa pizza')
    else:
        cantidad = int(input("Ingrese cantidad de pizzas: "))
        dic_orden[pizza] = cantidad
    seguir_pidiendo = input('¿Seguir ordenando?: ')
seguir_pidiendo = 'si'

#Con este bucle te pide que bebida quieres y la cantidad de esta, hasta que el usuario ya no quiera ordenar más (Se guarda en un diccionario)
while seguir_pidiendo.lower() == 'si':
    bebida = input('Ingrese que bebida quiere (Coca-Cola, Sprite, Inca Kola): ')
    if bebida not in Bebidas:
        print('Lo sentimos, no contamos con esa bebida')
    else:
        cantidad = int(input("Ingrese cantidad de bebidas: "))
        dic_orden[bebida] = cantidad
    seguir_pidiendo = input('¿Seguir ordenando?: ')

#Con esto separo el diccionario donde esta la orden en dos listas, una con la orden y otra con las cantidades
orden_pizzas_bebidas = list(dic_orden.keys())
orden_cantidad = list(dic_orden.values())

#Con esto se imprime tu pedio
print("Su orden sería:")
for i, j in dic_orden.items():
    print(i, ':', j)

# defino variables
precio = 0
indice_pizzeria = 0
indice_distrito = 0

#Con esto saco el indice del distrito
for i in range(len(Distritos)):
    if Distritos[i] == distrito:
        indice_distrito = i

from Funciones import cantidad_orden

#Con esto saco el indice de la pizzeria
for i in range(len(Pizzerias)):
    if Pizzerias[i] == pizzeria:
        indice_pizzeria = i

#Con esto hallo el precio (Multiplico las cantidades por los precios y lo sumo)
for i in range(len(Precios_pizza)):
    if i == indice_pizzeria:
        precio += Precios_pizza[i][0]*cantidad_orden(orden_pizzas_bebidas, orden_cantidad, 'Americana')
        precio += Precios_pizza[i][1]*cantidad_orden(orden_pizzas_bebidas, orden_cantidad, 'Hawaiana')
        precio += Precios_pizza[i][2]*cantidad_orden(orden_pizzas_bebidas, orden_cantidad, 'Pepperoni')
        precio += precios_bebida[i][0]*cantidad_orden(orden_pizzas_bebidas, orden_cantidad, 'Coca-Cola')
        precio += precios_bebida[i][1]*cantidad_orden(orden_pizzas_bebidas, orden_cantidad, 'Sprite')
        precio += precios_bebida[i][2]*cantidad_orden(orden_pizzas_bebidas, orden_cantidad, 'Inca Kola')

print('El costo por todo sería:', round(precio, 2), 'soles')
print('Llame al', Numeros_telefonicos[indice_pizzeria][indice_distrito], 'para efectuar su pedido')
